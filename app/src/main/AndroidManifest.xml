<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- BLE permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Network for Supabase -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/app_name"
        android:theme="@style/Theme.Gen3FirmwareUpdater">

        <!-- Registration Choice - LAUNCHER ACTIVITY -->
        <activity
            android:name=".RegistrationChoiceActivity"
            android:exported="true"
            android:clearTaskOnLaunch="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- User Registration (with scooter) -->
        <activity
            android:name=".RegisterUserActivity"
            android:exported="false" />

        <!-- Distributor Registration (with activation code) -->
        <activity
            android:name=".RegisterDistributorActivity"
            android:exported="false" />

        <!-- Login -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- Distributor Menu (after distributor login) -->
        <activity
            android:name=".DistributorMenuActivity"
            android:exported="false" />

        <!-- Scan for Scooter (PRIMARY distributor action) -->
        <activity
            android:name=".ScanScooterActivity"
            android:exported="false" />

        <!-- Scooter Selection/Inventory (browse all scooters) -->
        <activity
            android:name=".ScooterSelectionActivity"
            android:exported="false" />

        <!-- User Management (search, view, edit users) -->
        <activity
            android:name=".UserManagementActivity"
            android:exported="false" />

        <!-- User Detail (view/edit individual user) -->
        <activity
            android:name=".UserDetailActivity"
            android:exported="false" />

        <!-- Scooter Details (view history and customer info) -->
        <activity
            android:name=".ScooterDetailsActivity"
            android:exported="false" />

        <!-- Firmware Updater (after login) -->
        <activity
            android:name=".FirmwareUpdaterActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:keepScreenOn="true"
            android:configChanges="keyboardHidden|orientation|screenSize" />
    </application>
</manifest>
